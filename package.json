{
  "name": "chrono-date-parser",
  "version": "0.1.0",
  "author": "Lorenzo Mangani <lorenzo.mangani@gmail.com>",
  "scripts": {
    "build": "npm run clean && wasm-pack build --target nodejs",
    "clean": "rimraf dist pkg target",
    "test-rust": "cargo test && wasm-pack test --node",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "jest-cli": "^27.5.1",
    "rimraf": "^3.0.2",
    "wasm-pack": "^0.10.2"
  }
}
